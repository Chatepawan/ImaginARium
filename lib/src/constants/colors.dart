import 'package:flutter/material.dart';

// Primary color for main UI elements (e.g., buttons, headers)
const primaryColor = Color(0xFF744EE5);

// Accent color for highlights and secondary UI elements
const accentColor = Color(0xFF1A6CA8);

// White color for backgrounds, text, etc.
const whiteColor = Colors.white;

// Black color for text and dark elements
const blackColor = Colors.black;

// Background color for cards or sections
const cardBgColor = Color(0xFFF7F6F1);

// Optionally, define additional colors if you need them later
// const secondaryColor = Color(0xFF7A25BC); // You can use this for an additional primary-like color
// const lightGrey = Color(0xFFE0E0E0); // A light grey color for borders or subtle backgrounds
// const darkGrey = Color(0xFF333333); // Dark grey for text or icons
